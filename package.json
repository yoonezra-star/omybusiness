{
  "name": "omybusiness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static migration of omybusiness.com from Tistory to Cloudflare Pages.",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "build": "node scripts/build.js",
    "preview": "node scripts/serve.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
